# This example shows how to rename a single file in the doc site.
site_name: ok-with-rename-directory
plugins:
  - simple:
      semiliterate:
      - pattern: '^foo.bar$'
        destination_directory: baz
